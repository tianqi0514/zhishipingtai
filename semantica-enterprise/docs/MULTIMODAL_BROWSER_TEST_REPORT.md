# 多模态浏览器点击测试报告

测试日期：2026-09-03
地址：`http://localhost:8080/`
浏览器：Codex 内置 Chromium，真实页面与真实后台服务

## 已执行流程

| 页面 | 操作 | 结果 |
|---|---|---|
| 文档上传 | 选择真实 MP4 | 展示 15.9 秒、1280×720、预计 3 帧和云端关键帧提示 |
| 文档上传 | 点击取消 | 弹窗关闭；未提交；未出现“请选择文件” |
| 媒体策略 | 打开验收策略并点击校验 | ASR、Kimi Vision、抽帧预算均显示真实“就绪”状态 |
| 数据源 | 编辑 MinIO 音视频源 | 可配置媒体策略、四种抽帧方式、ASR/OCR/Vision、上限、失败策略和云端许可 |
| 数据源 | 点击取消 | 弹窗关闭；未提交；无告警 |
| 文档详情 | 打开视频媒体详情 | 显示播放器、100% 进度、1 段转写、1 个场景、2 个关键帧和 8 个结果页签 |
| 时间线 | 点击 7.958 秒关键帧 | 播放器真实跳转并随播放时间高亮当前范围 |
| 视觉理解 | 查看关键帧 | 展示真实 Kimi 场景摘要、可见文字和耗时 |
| 智能问答 | 新建会话并只选择音视频空间 | 检索范围保存为 1 个空间 |
| 智能问答 | 提问“视频中介绍了哪些数据接入方式” | DSH 41 秒真实完成，回答来自视频证据 |
| 执行过程 | 观察右栏 | 8 个真实事件、1 条检索轨迹、10 条排序召回 |
| 回答引用 | 点击 `[2]` | 自动切换召回依据并定位到 `video/frames/1/ocr · 7.958s` |
| 片段详情 | 点击“查看真实片段” | 展示文档、结构位置、时间、标签、OCR 原文和播放按钮 |
| 时间按钮 | 点击回答内 `▶ 00:07` | 打开权限校验后的当前站点视频并从指定时间播放 |
| 图谱 | 悬停多个画布位置 | 无 `undefined.z` 异常，控制台 0 错误 |
| 冷启动恢复 | 全栈无删卷重启后重新打开智能问答 | 历史问题、回答、269 个事件、10 条依据和计时均恢复 |
| 冷启动恢复 | 点击恢复回答中的 `▶ 00:07` | 权限校验通过，播放器从 7.958 秒附近开始播放 |

## 响应式与滚动

| 视口 | `document.scrollWidth` | 结果 |
|---|---:|---|
| 1280×720 | 1280 | 无页面级横向溢出，播放器和媒体页签可见 |
| 1440×900 | 1440 | 无溢出；文档、图谱和问答三栏正常 |
| 1920×1080 | 1920 | 无溢出；主内容区充分利用宽度 |

媒体结果、会话消息、右侧依据和表格使用各自容器滚动，不把页面无限撑宽。

## 控制台与网络

- 最终静态资源：`app.js?v=20260903-multimodal-2`、`graph3d.js?v=20260903-multimodal-2`。
- 新浏览器会话复测图谱悬停后，localhost 控制台错误为 0。
- 视频请求返回 `206 Partial Content`。
- 媒体画像、时间线、场景、关键帧和治理画像接口均返回 200。
- 无权用户对媒体资源的 403 是预期安全结果，不是前端异常。
- 冷启动复测时 `document.scrollWidth` 与 1440px 视口一致，浏览器控制台错误为 0。

## 测试数据保留

保留 `音视频验收数据集 20260902194901`，便于产品验收人员直接打开查看。临时权限测试用户已删除；API Key 和内部 Token 未在页面、截图或报告中展示。
