# 结构化数据语义层开发留痕

## 背景与边界

本分支从 `知识底座V1.0` 增量开发，没有推倒现有数据源、文档加工、Semantica、知识图谱或 DeepSeek Harness 链路。浏览器和 Harness 均只访问 FastAPI；Harness 不持有数据库连接信息，不直接访问 PostgreSQL/MySQL/OpenSearch/Qdrant/FalkorDB。

Ontology2SQL 锁定在 `ece05d1cc988d9bce602a7a9e1b73cd5767a860a`，用于严格 Plan/IR 与确定性编译的设计参考。生产实现按本平台已有模型、权限和 MySQL/PostgreSQL 方言重写，没有复制 BIRD Gold SQL、Oracle Evidence 或 SQLite 编译器。

## 实施阶段

| 阶段 | 主要产物 | 验证 |
|---|---|---|
| 设计 | `STRUCTURED_SEMANTIC_QUERY_DESIGN.md` | 首个独立设计提交 `65f685a` |
| Schema Registry | Schema 版本、对象、字段、Fingerprint、Diff、漂移状态 | 单元、真实 MySQL/PostgreSQL、API E2E |
| 数据预览 | 实时/快照、分页、排序、类型筛选、服务端脱敏、行详情 | 真实浏览器点击和数据库集成 |
| 本体映射 | 映射集/版本/实体/属性/关系、建议、验证、激活、回滚 | API E2E 与 UI 工作台 |
| 查询安全层 | 严格 Plan、严格 IR、MySQL/PostgreSQL 编译器、只读执行器 | 数值矩阵与注入测试 |
| DSH 集成 | 5 个结构化工具、结构化事件、引用、取消与恢复 | 15 项 DSH 合约和真实多轮会话 |
| 快照/图谱 | 行级 ContentElement、稳定 Hash、映射驱动物化 | 单元与 Semantica 合约 |
| 迁移 | `0017_structured_semantic_query.sql`、`0018_schema_fingerprint_history.sql` | 已有数据库升级、重复启动、冷构建 |
| 交付 | Compose fixture、验收种子、README、专题文档、测试报告 | 三轮回归 |

## 关键缺陷与修复

### 1. Schema Fingerprint 恢复冲突

初版把 `(source_id, schema_fingerprint)` 设为唯一。真实漂移测试发现，当 V1 → V2 → 恢复为与 V1 相同结构时，V3 无法写入，破坏 Schema 历史语义。

修复：

- 删除 ORM 唯一约束。
- 增加 PostgreSQL 专用迁移 `0018` 删除旧约束。
- 迁移执行器支持 `-- dialect:<name>` 语句前缀。
- 增加“相同 Fingerprint 可在更晚版本再次出现”的单元和 API E2E。

### 2. DSH 取消后 SDK 初始化竞态

真实浏览器点击停止后立即重试，曾出现旧 SDK client 关闭尚未完成而新 client 初始化超时。

修复：

- 用 `closingSessions` 串行化同一 Session 的关闭和重建。
- 取消统一进入 `disposeSession`。
- `turn/start` 前失败会驱逐半初始化 client。
- 初始化默认超时调整为 90 秒，同时保留环境变量覆盖。
- DSH 源码合约增加取消清理和初始化失败驱逐断言。

实际重试和后续多轮查询均成功。

### 3. 对话列表摘要为空

最近一条失败/取消的 assistant 消息可能内容为空，导致会话列表显示“暂无消息”。

修复：列表投影选择最近一条非空消息；新增投影单元测试。

### 4. 运行中计时不同步

右侧执行过程计时正常，但消息卡片一度停留在 0.0 秒。

修复：同一个 `chatTurnTiming` 每秒同时更新消息卡片和右侧执行过程；浏览器实测 2.5 秒 → 25 秒 → 完成 40 秒。

### 5. 登录 UI 合约断言陈旧

前端已支持字符串、JSON 字符串和结构化 `detail.message`，旧测试仍匹配已移除的一行三元表达式。

修复：保留更健壮的生产逻辑，更新测试检查新的 detail 归一化路径，不降低业务断言。

## 复用 Semantica

- DBIngestor 与数据库接入边界。
- 统一 ContentElement、结构化解析、Normalizer、Provenance。
- 现有治理、冲突、GraphRelease 与 FalkorDB 发布。
- 现有 Chunk、索引发布和 SearchRanker，不复制算法。

本分支新增的是组织权限下的 Schema Registry、映射控制面、实时只读查询安全层和 DSH 工具协议。

## 最终状态

- 全部必需容器健康。
- 无删卷重启后业务数据、映射、QueryRun、会话和 DSH Session 恢复。
- 最终页面控制台 0 错误，核心服务日志无未处理异常。
- 自动化和浏览器结果见 `STRUCTURED_QUERY_TEST_REPORT.md` 与 `BROWSER_TEST_REPORT.md`。

## 2026-09-02 全业务平台回溯

在 `codex/full-business-platform-validation` 分支上按集团真实使用顺序重新串联全部模块，没有推倒 Semantica、结构化语义层或 DSH 适配。

主要增量：

1. 建立集团组织、角色、空间、多模态资料、双方言经营数据库和固定标准答案。
2. 验证并补齐数据库源生命周期、Schema 漂移、映射失效、图谱物化删除、人工治理回滚和应用反馈复测闭环。
3. 为 REST、MCP 和 CLI 补齐结构化安全查询入口，三者仍只访问 FastAPI。
4. 用确定性 Query Policy 区分文档说明、实时数值与混合问题；在 FastAPI 可信边界强制激活 Metric Contract。
5. 把引用编号固化为检索片段/数据查询的不可变外键，并在回答完成前校验。
6. 修复冷启动验收对额外测试容器的错误假设，并通过真实 Retry API 处理外部模型瞬时 429。
7. 把审计日志从技术码、UUID、JSON 改造成中文业务视图，内部标识默认不展示。
8. 更新静态资源版本指纹，避免部署新镜像后浏览器继续使用旧 JS/CSS。

最终功能提交为 `95666ecb99b99678909b4b8a7e5995358220f533`。平台 229 项 Python 测试全部实际执行通过，DSH 16/16，Ontology2SQL 192 passed/8 xfailed；14 个验收容器全部健康。完整证据见 `FULL_PLATFORM_VALIDATION_20260902.md`。

## 2026-09-03 多模态音视频流水线

分支 `codex/multimodal-media-pipeline` 从设计提交 `a72aaa5` 开始，按“策略控制面 → 处理流水线 → 时间线与引用 → 数据源 → Docker 模型 → 真实验收”的顺序增量开发。

主要实现：

1. 媒体策略 CRUD、版本、校验、克隆、使用关系、空间/数据源/上传优先级和不可变版本快照。
2. 固定间隔、固定 FPS、场景变化和智能组合四种抽帧；时段、预算、pHash 去重、开始/结束帧和帧级重试。
3. 独立 FunASR/SenseVoice CPU 服务，本地真实中文转写；静音、损坏、无音轨和取消边界。
4. Tesseract OCR 与 Kimi K3 严格结构化视觉理解；关键帧级云端确认、审计、并发、超时和降级。
5. 音视频统一 ContentElement、时间线、Range 播放、检索时间字段、片段弹窗和回答内时间按钮。
6. S3/MinIO、目录、邮件和归档媒体成员拆分为独立文档；媒体清单父快照保留来源历史而不重复加工。
7. 处理指纹、全量缓存和阶段缓存；失败重试、场景重试、停止、稳定元素 ID 和软删除。
8. Docker 增加 ffmpeg/ffprobe、Tesseract 中文英文语言包、libmagic、字体和独立 ASR 镜像/模型缓存 Volume。

真实缺陷修复包括 OCR Path 兼容、重处理外键、模型测试导致缓存漂移、缓存命中重复知识任务、纯媒体源父快照失败、历史失败同步恢复、图谱悬停空投影、时间轴活动状态和不可点击时间链接。

最终验收数据和测试证据见 `MULTIMODAL_MEDIA_TEST_REPORT.md` 与 `MULTIMODAL_BROWSER_TEST_REPORT.md`。本地 1B–2B 视觉模型因当前 16GB 开发机资源边界未标记为真实运行；协议适配和明确降级已完成。

第三轮无删卷冷启动于 2026-09-03 完成：13 个必需服务全部健康，媒体处理结果、DSH Session Event、引用和检索索引均恢复；冷启动后 10 项媒体专项回归及浏览器时间引用播放再次通过。启动峰值期间发现 Docker Desktop 对两个旧容器的 `exec` 命名空间失效，已通过仅重建容器（保留 Volume）处理并记录，未发现业务数据损失。

## 2026-09-03 短文档知识加工性能修复

对 `传神AI配音案例分析.docx` 的真实任务数据进行逐步骤定位：解析约 1 秒，旧语义抽取按 26 个 Chunk 串行调用 Kimi K3，知识加工总耗时 20 分 53 秒。增量修复如下：

1. 仅在模型请求边界组合相邻短 Chunk，检索 Chunk、ContentElement 和引用粒度保持不变。
2. 按模型配置启用有界并发，平台上限为 8。
3. 模型输出必须依据原文映射回原始 Chunk，无法锚定的输出拒绝持久化。
4. 任务详情新增模型请求批次、成功批次、并发、失败 Chunk 和未锚定项目指标。
5. `partial`、`partial_failed` 和 `cancelled` 步骤正确记录结束时间。
6. 修复中文文件名下载触发 Starlette `latin-1` 编码异常的问题，使用 ASCII fallback 与 RFC 5987 `filename*`。

同文档、同模型真实回归：26 个原始 Chunk 保持不变，模型请求从 26 次降到 3 次，知识加工从 20 分 53 秒降到 5 分 15 秒，24 个 Chunk 抽取成功，全部持久化的实体、关系和事件均能回溯到该版本原始 Chunk。完整说明见 `KNOWLEDGE_PROCESSING_PERFORMANCE.md`。

## 2026-09-03 通用弹窗表单隔离修复

用户在“空间与目录”新增空间时，输入字段后保存按钮被错误禁用。根因是治理画像等功能通过 `addEventListener` 注册在共享 `#modal-form` 上的输入监听器，在弹窗关闭后仍残留，并影响后续空间、用户、角色和配置弹窗。

通用 `modal()` 现在每次打开都会克隆并替换表单节点，在绑定当前弹窗事件前移除所有上一弹窗遗留的 DOM 监听器；关闭、取消和保存按钮也只绑定到当前表单。静态资源版本已更新，避免浏览器继续使用缓存脚本。

浏览器真实验证完成：输入编码、名称、描述后保存按钮持续可用；测试空间真实创建并显示；取消不提交；测试数据随后清理。页面控制台 0 错误，完整 Python 回归 245 passed、15 skipped。

## 2026-09-03 全局知识空间上下文与产品文案收口

把知识空间从各模块内重复、容易选错的筛选项提升为左侧栏品牌区下方的唯一业务上下文。当前选择按用户持久化；切换后工作台、资产总览、文档、数据源、治理、任务、图谱、检索、问答和知识分析均重新读取同一空间的数据。空间与目录继续作为空间管理入口，平台级配置、应用构建和系统管理不被错误套用单空间过滤。

后端为工作台和任务列表增加经权限校验的 `space_id`，文档和数据源继续使用已有服务端空间筛选；会话、治理、图谱和分析沿用原有空间权限接口。上传、新建数据源、新建会话和分析运行不再让用户重复选择空间，而是显式展示并提交侧栏当前空间。

产品界面中的 `Semantica` 与 `Semantic Query Plan` 文案统一改为“语义建模”与“语义建模查询计划”。内部 Python/JavaScript 标识、数据库字段、兼容 API 路径以及知识文档自身包含的厂商名称保持不变，避免破坏协议和篡改知识内容。

验证结果：容器内 229 项单元测试通过；真实 API 验证工作台、文档、数据源和任务均按空间过滤；浏览器完成空间切换、刷新恢复、文档列表、数据源、治理、图谱、问答会话、知识分析、任务中心和平台级配置边界检查，控制台无错误。

根据页面视觉复核，取消位于页面顶部正中的大面积空间按钮，改成与主导航同属一个业务层级的蓝白侧栏入口。入口支持空间名称/编码搜索、当前项标记、点击外部或 Escape 关闭；侧栏折叠后只保留空间首字图标并向右展开菜单。平台级页面在相同位置显示灰色“平台级管理”，顶部只保留页面标题和当前操作。浏览器在 1280×720、1440×900 以及展开/折叠状态下复测无横向溢出，控制台无错误。

## 2026-09-04 知识分析业务化重构

从 `codex/multimodal-media-pipeline` 的 `c68d728c476a2c8483c8e99944bb45d203e18291` 创建 `codex/knowledge-analysis-business-ux`，仅增量调整知识分析及其必需接口。

主要实现：

1. 将规则引擎式信息架构重组为“分析任务、发现结果、规则库、专业查询”，默认从业务问题和当前空间准备状态开始。
2. 增加数据准备、关系词表、业务模板、真实匹配预览、原子向导创建、任务运行、零结果诊断、运行比较和发布影响接口。
3. 五步向导使用业务角色和当前空间真实关系，提交时映射为安全 Datalog 变量；正式匹配和运行继续调用 Semantica `DatalogReasoner`。
4. 发现结果展示新增、保持、失效结论及证据链；预览、加入知识库和撤回使用真实 Worker 与图谱版本。
5. 规则库默认隐藏 DSL，增加自然语言版本前后对比；存在任务依赖时阻止删除规则组并返回受影响任务。
6. 专业查询默认进入受控普通查询；SPARQL 图谱投影为关系 URI 增加中文 `rdfs:label`，修复业务名称无法匹配的问题。
7. 建立独立“知识分析验收空间”，包含 2 份文档、7 个对象、5 条前提关系，证据覆盖率 100%。
8. 恢复因测试数据库容器停运而无法连接的 `经营数据验收库 f4356187`，验证 7 张表、42 个字段和实时预览；未修改其凭据。

验证结果：239 项全量单元测试、19 项合约测试和 2 项 live API E2E 全部通过。真实链路覆盖两条件制度适用、三条件风险传导、来源片段、正式发布、FalkorDB 查询、SPARQL、撤回和缺失关系诊断。详细证据见 `KNOWLEDGE_ANALYSIS_TEST_REPORT.md` 与 `KNOWLEDGE_ANALYSIS_BROWSER_TEST.md`。

## 2026-09-04 文档检索/图谱加工目标

针对文档知识加工长期停留在 `semantic_extract` 的问题，读取任务步骤和 Worker 源码后确认：该步骤是模型驱动的实体、关系、事件抽取，是图谱加工链路，不负责向量生成。最近一份 266.16 秒任务中该步骤耗时 181.07 秒，为主要瓶颈。

本轮增加 `vector`、`graph`、`both` 三种版本级加工目标，并保持 `both` 为兼容默认值。API 上传、自动派发、手工加工、Worker、索引发布和前端均使用同一持久化目标；图谱专用文档会被索引快照确定性排除，检索专用文档不会创建抽取运行、事实或图谱发布。任务页面使用业务名称展示步骤及真实跳过原因。

建立“知识加工方式验收空间”并真实上传三份文件。最终镜像回归中，仅检索、仅图谱、同时加工分别耗时 42.66、45.69、63.36 秒；模型抽取请求数为 0、1、1。API 验证三种投影隔离、完成状态、来源片段和召回差异；250 项单元测试与 19 项 Semantica 合约测试通过；浏览器验证模式默认值、切换、取消、手工提交、步骤说明和 1280 宽度无横向溢出，控制台无错误。额外修复了失败前误标完成、待发布版本串入其他快照、强制加工只刷新单侧投影和失败孤立实体污染等发布边界。

## 2026-09-04 业务旅程 UI/UX 与知识治理整合

从 `codex/document-processing-targets` 的 `a355e5f1eb5b97e78949623516dfffb8db4662a3` 创建 `codex/business-journey-ui-ux`。本轮只调整产品信息架构、前端请求生命周期、必要的治理概览接口和知识加工状态真实性，不改变 Semantica、DeepSeek Harness 或存储组件的核心边界。

主要实现：

1. 左侧收敛为九个业务域；知识图谱和规则推演归入知识治理，原 Hash 与接口保持兼容。
2. 新建空间后自动切换当前空间并进入文档列表；上传、新建数据源、不同知识加工方式完成后都提供真实下一步。
3. 新增治理概览和发布记录，聚合真实文档、加工目标、实体事实、治理、规则、任务和发布版本。
4. 建立视图请求取消、请求序号防竞态、公共请求去重、短期缓存、stale-while-revalidate、写后精确失效、滚动恢复和生命周期诊断。
5. 页面切换先显示结构骨架，图谱、SSE、轮询和计时器离开页面时统一清理。
6. 修复图谱语义抽取部分失败仍把任务标为成功的问题；错误现在以可重试失败呈现，只有图谱目标成功才触发规则推演。

真实验证：三种加工方式分别用时 41.66、60.45、67.00 秒，模型请求数为 0、1、1，投影隔离与召回差异全部通过。浏览器完成新空间自动跳转、上传取消、治理五个入口、白底 3D 图谱、前进后退、刷新恢复、20 次连续导航和两种分辨率回归，Console 0 错误。性能由旧版一次切换约 3068 ms、6 次公共请求，改善为核心页面 3–167 ms、单次重复请求 0；运营中心真实接口为 850–1070 ms。

## 2026-09-06 国联集团完整平台演示准备（进行中）

从 `86e9a414c630a449296cd2761fbee66099cae06b` 创建 `codex/guolian-full-platform-demo`，建立独立 `guolian-enterprise-demo` 空间、确定性业务故事、演示 Fixture、双数据库、治理与规则推演数据，以及 20/45/90 分钟脚本。截至本阶段快照，最终 commit、最终镜像和三轮放行尚未完成；最终状态见本节后续记录。

已取得的真实证据：

1. 23/23 份主演示材料已发布；Markdown 使用文本解析；扫描 PDF 使用 OCR；WAV 形成 5 个带时间区间的转写片段；MP4 形成 4 个转写片段、3 个带视觉描述关键帧、1 个场景和 10 条时间线。
2. 独立 PostgreSQL/MySQL Fixture 各为 16 张表和 1 个无主键视图；20 个标准答案分别真实实算 20/20。当前平台 Schema 快照仍只包含 16 表，需重新发现后验收视图。
3. 本体当前 34 个词条，图谱投影 290 个实体、384 条事实，发布序号 R59。为减少自动抽取产生的精确重名节点，通过真实治理发布合并 10 组“其他”类型重复实体；模糊别名仍留给业务人员判断。
4. 三个分析任务由 `semantica.reasoning.DatalogReasoner` 真实运行成功，合计 7 条预览结论，三条目标结论及其全部前提均有来源 Chunk。
5. 人工治理真实创建 11 条验证性决定、发布投影并批次回滚，最终 11 条均为 `rolled_back`；带来源的 `NexusOne—用于→智慧流程中枢项目` 关系作为主演示依赖保留。
6. 全文、向量、图谱和混合检索各有真实结果；REST、MCP、CLI 均完成过一次真实调用，DSH 产生多 Step/工具事件和引用。最终镜像后仍需重跑固定八轮与完整工具清单。
7. 模型路由实测为：四个核心 LLM 场景使用在线千问 Qwen3.5 Plus，Vision 也使用千问对应配置，本地 BGE 与 SenseVoice 可用；Kimi LLM/Vision 当前失败且不路由；内网 Qwen 停用；Reranker 未配置、检索使用 RRF。

本轮已定位并增量修复历史失败任务误计为当前异常、数据库权威品牌名被归一化为 `Nexusone`、图谱分页/大小写等问题。规则推演“页签可见但主体空白”进一步定位为图谱覆盖层 N+1 查询和内部页签异步竞态，源码已改为批量解析覆盖层、显示加载态并中止过期请求，专项单元测试 17/17 通过。截至本阶段快照，当前运行镜像尚未包含全部修复；七类无账号来源、20 题平台结构化查询全链、浏览器全菜单、三轮连续彩排和远端独立运行仍待完成。后续完成状态见“最终完整预检与浏览器关键链”。

标准 Dockerfile 的本机全新构建受缺失的 Semantica 基础镜像和 Docker Hub 网络超时阻塞；已有镜像上的增量验证不能写成从零构建通过。完整过程状态、测试分层和诚实边界见 `docs/demo/DEMO_TEST_REPORT.md`、`docs/demo/DEMO_BROWSER_TEST_REPORT.md` 与 `docs/demo/DEMO_KNOWN_LIMITATIONS.md`。

### 2026-09-06 演示图谱事实与证据对齐

复核演示推演链时发现：历史准备逻辑在找不到指定来源时会回退到文档首个 Chunk，部分系统依赖事实复用了并不支持该关系的通用 Chunk；“负责”还曾以“项目→负责→责任组织”反向登记。旧 Run 虽然每个前提都有 `source_chunk_id`，但这只能证明引用存在，不能证明引用文本支持事实，因此不再作为证据链通过记录。

增量修复如下：

1. 在机器 Ground Truth 中明确标注 9 条图谱种子事实，其中 8 条为三个 Semantica 场景的必要前提；每条声明唯一关系、来源文件、精确 Chunk 路径和主体/关系/客体证据词。
2. 新增 `规则推演前提事实（演示版）.txt`，补齐组织管理和系统依赖/组件维护链的显式来源；主演示素材由 23 增至 24，安全 ZIP 由 9 增至 10 个条目。
3. 准备脚本在写入任何图谱对象前逐条解析当前已发布 Chunk，完全移除“首个 Chunk”回退；任一文档、结构位置或证据词不匹配即硬失败。
4. 统一责任关系为“数字科技公司→负责→智慧流程中枢项目”。历史反向边通过治理覆盖层标记 `suppressed`，不硬删除；正确事实若引用了错误 Chunk，也通过带原因的人工治理覆盖层修正并保留审计。
5. 事实证据更新接口只接受同租户、同空间、当前文档版本的已发布 Chunk，并强制填写治理原因。跨空间、跨租户、未发布和旧版本证据均拒绝。
6. 预检新增“事实→Chunk 语义 Ground Truth”硬门槛，要求 9/9 条种子事实和 8/8 条推演前提全部一致、反向责任边为 0；报告只记录事实 ID 与失败原因码，不复制证据原文或敏感内容。

容器内聚焦测试覆盖编排 fail-closed、Ground Truth Schema、预检契约、权限边界、证据覆盖审计和软屏蔽历史关系，当前已通过。平台 24/24 上传、9/9 证据契约和三个 Semantica 场景的新 Run 仍须在重建镜像后真实执行，不能沿用改造前的成功记录。

### 2026-09-06 演示闭环复测快照

保留上述历史过程记录不改写。其后在当前本机离线增量镜像中取得以下新证据：

1. 24/24 份主演示材料当前版本均已发布并生成治理画像；9/9 条图谱种子事实与 8/8 条规则推演前提通过精确文档、Chunk 路径和证据词契约，历史反向责任边不再进入当前图谱。
2. 三个分析任务基于新证据契约由 `semantica.reasoning.DatalogReasoner` 真实重跑成功，共形成 7 条结论并命中 3/3 个预期业务结论；当前浏览器已打开供应商风险结果、三前提证据链和真实来源片段。
3. 独立 PostgreSQL/MySQL 的 20 个 Ground Truth 分别保持 20/20；同一 20 题还通过平台公开自然语言问题→Plan→严格 IR→参数化 SQL→只读执行链路，结果为 20/20。两库结构发现和无主键对象检查已重跑。
4. 七类无账号数据源完成 7/7 连接、发布及第二次同步 `unchanged`。当前图谱投影快照为 338 个实体、522 条事实；这是可变化的运行快照，不写成固定产品容量。
5. 跨文档完全/近重复治理增加确定性扫描和业务待办：可比较 A/B 当前版本、保留两份，或选择主文档并屏蔽副本文档当前版本 Chunk/Fact；屏蔽后真实重发全文、向量和图谱，合并批次可回滚并重新打开待办。Document 和历史版本不物理删除，“保留两份”当前没有独立撤销入口。
6. 仅全文、仅向量、仅图谱、全文+向量、三路，以及三路并请求重排共六种模式均已真实调用。当前未配置 Reranker，第六组明确 Warning 并按 RRF 降级，不能表述为执行了二阶段重排。
7. 固定 8 轮 DeepSeek Harness 对话完成并恢复历史，覆盖指代、结构化查询、文档+数据、图谱、多模态、引用追问和证据不足；服务链检查同时确认 MCP 11 个工具可见且原始 SQL 被拒绝。
8. 规则推演空白与工作台异常任务误计修复已部署到当前离线增量镜像；浏览器显示 338 个对象、522 条事实、3 条规则，工作台当前异常任务为 0。

截至本阶段快照仍不是最终放行：最近一次完整预检在向量重试修复前被单通道瞬时超时阻断；修复后向量专项返回 10 条、29 个候选且无 Warning，但当时还需重跑一次完整预检。全菜单浏览器矩阵、三种分辨率、三轮连续彩排、不删 Volume 冷启动和远端独立运行当时仍未完成。标准 Dockerfile 的从零 clean build 当时继续受 Registry/Docker Hub 网络影响；当前离线增量镜像包含修复并可运行，但不能替代 clean build 证据。制度谱系/条款差异、过期知识自动下线、非结构化文档统一 PII 发布拦截仍是明确产品边界。最终预检及不删卷恢复状态见下文。

### 2026-09-06 最终完整预检与浏览器关键链

保留前述阶段快照不改写。向量搜索增加配置化超时和幂等瞬时重试、Dashboard 与治理概览统一当前版本失败口径后，重新执行最终完整预检。本次运行耗时 284.99 秒，结果为 `ready=true`，52/52 个 required 检查全部通过。

本轮新增真实证据：

1. 24/24 份主演示材料当前版本已发布且 24/24 有治理画像；七类无账号数据源 7/7 完成连接、发布和第二次同步 `unchanged`。
2. PostgreSQL 与 MySQL Ground Truth 各 20/20，共 40/40；平台公开自然语言问题→Plan→严格 IR→参数化 SQL→只读执行链路为 20/20。
3. 当前图谱投影为 338 个实体、522 条事实；三个 Semantica 场景 3/3 成功，形成 7 条结论，三个预期结论 3/3 命中且有来源证据。
4. 仅全文、仅向量、仅图谱、全文+向量、三路和三路并请求重排六种检索模式均通过。当前未配置 Reranker，请求重排时明确 Warning 并继续采用 RRF，不表述为二阶段模型重排。
5. 服务链真实耗时：REST 3.716 秒、DeepSeek Harness 69.530 秒、MCP 35.347 秒、CLI 36.543 秒；MCP 暴露 11 个工具，原始 SQL 请求被拒绝。
6. 人工治理支持矩阵 8/8；跨文档重复待办可查看 A/B 当前版本、选择主文档并屏蔽副本当前 Chunk/Fact、重发检索/图谱及批次回滚。Document 和历史版本不物理删除。
7. 当时的 Python 单元测试快照为 443/443，DeepSeek Harness 契约测试 18/18；最终生产镜像重建后的完整数字见本节末尾“最终生产镜像与交付复核”。

浏览器真实关键链取得以下证据：

1. 登录、全局空间选择和空间联动正常；工作台显示 0 个当前异常任务，治理概览与当前演示空间一致。
2. 跨文档重复待办可打开 A/B 内容；规则推演首页、供应商风险结果、三前提证据链和真实来源片段可用。
3. 智能问答真实生成耗时约 134 秒，页面展示 23 个可核验执行事件；本轮全文候选 29、向量候选 29、图谱候选 26，去重前 90、去重后 30，最终展示 10 条排序依据和 5 个有效引用；引用 1 打开真实 EML 片段。
4. 检索通道不能全部关闭；REST、MCP、CLI、Agent 四类服务开放手册均可打开。
5. PostgreSQL 实时预览、行加工状态、无主键 Warning 和空表状态均真实显示。
6. 白底 3D 图谱显示 338 节点、522 事实；缩放、适配、旋转和搜索定位实际点击可用。
7. 1280×720、1440×900、1920×1080 三种视口均无非预期横向溢出；智能问答三栏保持独立纵向滚动；本轮已执行路径 Console 为 0 个应用错误。

此阶段之后已完成生产 Dockerfile 的 Linux/amd64 干净构建，见本节末尾“最终生产镜像与交付复核”。其余诚实边界保持不变：远端测试环境尚未独立验证；Kimi、内网 Qwen 和 Reranker 当前不可用；制度谱系/条款差异、过期知识自动下线、非结构化 PII 发布阻断尚未闭环。三轮完整客户彩排也尚未宣称完成。本次结论只更新为“本机最终预检和浏览器关键业务链通过”，不提前扩大为远端或整体交付放行。

2026-09-06 最终收口阶段在同一生产镜像与持久化数据上又连续执行两次完整自动预检。连同此前最终验收，共三次均为 `ready=true`、52/52 required；每次均重新执行双库 Ground Truth 40/40、公开结构化查询 20/20，以及 REST、DeepSeek Harness、MCP、CLI 真实服务链。后两次服务链总耗时分别为 129.514 秒和 160.601 秒。该证据用于确认重复执行稳定性，但不替代三次完整人工客户彩排；最终镜像浏览器关键链目前真实点击一轮。

### 2026-09-06 不删 Volume 重启与远端复核

在最终预检后正常停止并重新启动 API、Worker、Scheduler、Agent Runtime 和 MCP，未删除任何 Volume。全部必需容器恢复 `healthy`；重启后仍读取到演示空间 33 份文档、338 个实体、522 条事实和 3 个会话，最近会话恢复 2 条消息与 113 个事件。重启后的仅向量检索返回 5 条结果、15 个候选且无 Warning；浏览器刷新后登录、当前空间和图谱 R85 恢复，Console 仍为 0 个应用错误。

对远端测试环境进行只读网络复核：主机可 Ping，9001、9002、9003、9010 可建立 TCP 连接，但 SSH 22 超时，业务端口在 HTTP、HTTPS 和 SSH banner 探测中均未返回应用层数据。当前无法登录读取 Docker 状态，目标 HTTP 地址也无首字节响应，因此明确记录为“远端不可用/未完成独立部署验证”，不宣称远端已交付。

### 2026-09-06 最终生产镜像与交付复核

完成生产 Dockerfile 的 Linux/amd64 干净构建并重建应用服务。最终镜像为 `semantica-enterprise:0.10.0`，摘要 `sha256:441f39ee40a1b743dc8c277edf5cd32a18f1b53e8b724915276002af9a423c93`；以非 root `app` 用户（UID 10001）运行，`pip check` 通过。镜像内 Semantica 0.6.6、torch 2.13.0+cpu、Docling 2.124.0 可导入，DOCX 转换、OCR 和 `ffprobe` 实际检查通过。API、Worker、Scheduler、MCP 已由该镜像重建，Agent Runtime 使用独立最终镜像；最终收口时 16 个正式演示容器全部健康，15 个重启计数为 0。ASR Runtime 在启动早期累计重启 10 次，随后自 17:09 CST 起稳定运行，并连续通过模型检查、真实转写和三次完整自动预检；已在测试报告与已知限制中如实记录。

最终测试证据：Python 共收集 495 项，478 passed、17 skipped、0 failed；17 项为默认环境门控，并由单独 live 运行覆盖：两个 analysis live 与 PostgreSQL Fact 合计 3/3、结构化数据库 10/10、结构化 API live 1/1。DeepSeek Harness 契约测试 18/18。最终完整预检共 54 项，52/52 required 通过；PostgreSQL/MySQL Ground Truth 各 20/20，平台结构化 API 20/20；REST、DSH、MCP（11 个工具且原始 SQL 被拒绝）和 CLI 均通过。

最终镜像浏览器复测覆盖白底图谱、规则推演、智能问答三栏与真实执行过程、召回依据、真实片段、“返回最新回答”、数据库实时预览和服务端脱敏、文档列表；已执行路径 Console 为 0，最终停留 `#dashboard`。这不是对治理、全部管理菜单及所有按钮组合的穷举，也不替代三轮完整客户彩排。

2026-09-06 17:25 CST 再次复核远端：`121.196.149.55:22` TCP/SSH 超时；9002 TCP 约 34 ms 可建立连接，但 `/` 与 `/health/ready` 均在 12 秒内返回 0 bytes、HTTP 000。当前仍无法验证远端部署。Kimi 当前不可用、内网 Qwen 保持停用、Reranker 未配置；这些边界不因本机最终镜像通过而改变。
