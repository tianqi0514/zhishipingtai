# 可控推演写作浏览器测试报告

## DeepSeek Work 最终只读验收

- 地址：`http://10.5.113.232:18990/`
- 新建全新会话，页面模型选择器显示 `deepseek-v4-flash-0731`。
- 输入仅允许调用 `chuanshen_writing_project_context` 的只读指令。
- 页面真实显示“1 次工具调用 · 1 条消息”，47 秒完成。
- 返回科研楼可研项目 `54c654c2-1788-44c1-9472-5d2f1b37338d`：13 条已核验事实、1 份文稿、0 个待处理闸门。
- 回答明确未创建、修改、应用影响、导出或发布。
- 本轮浏览器控制台 `error` 数量为 0。

页面日志里保留了一组页面启动时的连接重试 Warning，均发生在同一旧时间点；当前会话随后正常建立连接、流式完成并收到终态。这不是本轮工具调用失败，但仍作为稳定性观测项保留。

## DeepSeek Work 写作回归证据

既有真实会话中重新核验了以下可见结果：

- 确定性判据成功消费震级和人口密度事实。
- Semantica 预览运行使用 `semantica-datalog 0.6.6`，具有真实运行 ID、两个前提和一条推导结论，未发布。
- 单章写作真实完成：`completed/100%`，1/1 章节，10 个 Plate 顶层块，4 个知识引用绑定和 4 个测算绑定。
- 单章质量门 `ok=true`，无未绑定权威数字；完整文稿校验仍会如实指出其余未生成章节。

以上历史会话用于回归插件写作能力；本轮最终全新会话只执行只读工具，避免为了截图再次制造文稿数据。

## 传神智库 / 妙笔入口

- 无登录态访问 `http://10.5.113.232:9002/miaobi/` 会回到统一登录页，未绕过认证。
- 本轮没有把密码复制进自动化脚本，也没有在报告中保存凭据。
- 既有妙笔浏览器回归已覆盖 Plate 正文、Chunk 依据、指标弹窗取消、影响预览、全选/逐项应用、版本差异和导出。
- 1280×720 与 1440×900 基线无整页横向溢出；本轮后续提交没有修改前端布局。

## 结果分级

| 项目 | 结论 |
|---|---|
| DeepSeek Work 模型选择 | 通过，真实显示 `deepseek-v4-flash-0731` |
| 插件只读工具调用 | 通过 |
| 页面真实终态 | 通过 |
| 当前会话 Console Error | 0 |
| 未登录访问控制 | 通过，返回登录页 |
| 启动瞬间连接 Warning | 非阻塞，保留观察 |
| 本轮浏览器整篇长报告重生成 | 未重复执行；由服务器 API 全链路与既有完整报告回归覆盖 |
