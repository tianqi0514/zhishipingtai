# 可控推演写作测试报告

## 结论

本轮在 `codex/controlled-writing-bidirectional-diff` 上完成语料包、脱敏 Skeleton、继承 Diff、编辑 Diff、确定性传播闭包、影响预览、选择性应用、回滚、文稿版本和正式导出的实现与回归。DeepSeek Work 插件位于独立分支 `codex/controlled-writing-plugin`，只通过传神智库 API 工作，不直连数据库或知识中间件。

本报告区分三类证据：代码自动化、服务器真实 API 验收、真实浏览器验收。测试数据均为构造的业务测试数据，不代表真实灾情或真实项目投资。

## 自动化结果

| 测试层 | 结果 | 说明 |
|---|---:|---|
| 受控写作、妙笔、写作图谱、API、Semantica 综合套件 | 235 passed，2 skipped | 跳过项为环境门控；无失败 |
| Semantica 定向合约 | 67 passed | 包含 Datalog、证明链和输入变化重推演 |
| 妙笔前端组件 | 42 passed | 后续改动均为后端和导出修复，前端构建基线保持通过 |
| DeepSeek Work 插件 | 8 passed | 46 个唯一工具、严格 Schema、错误标准化和构建检查 |

综合套件已包含部分定向用例，以上数字不简单相加为“总测试数”。第一次综合回归发现两个真实兼容问题：旧 `/documents/{id}/recompute` 无法沿未显式 predecessor 绑定的新事实找到历史依赖，以及 API 暴露内部 `depth` 字段。修复后针对性测试和完整综合套件均通过，没有放宽断言。

## 服务器真实验收

最终证据文件：

`/home/tianqi/controlled-writing-validation/controlled-writing-live-20260919174848-d8bcf4.json`

### 应急资源联动

- 项目：`6b6b62d8-eb3b-4e8e-8e7f-a7dc156f497d`
- 语料包产物映射：25 项；Skeleton：1；历史值暴露：否。
- 已核验事实：9；确定性判据：2。
- Semantica：`semantica-datalog 0.6.6`，推导结论 1 条。
- 正文 Chunk：6；绑定：6。
- 影响正文块：6；传播证明路径：8。
- `可用人员 320→400` 后，缺口由 `180→100`。
- 完成全部应用、回滚、逐项应用；未选择的 4 个 Chunk 保持旧值并标记 stale。
- 历史事实、计算运行和文章版本未被覆盖。

### 科研楼可研联动

- 项目：`54c654c2-1788-44c1-9472-5d2f1b37338d`
- 语料包产物映射：25 项；Skeleton：1；槽位：19；历史值暴露：否。
- 确定性计算运行：6。
- 缺少建设期利息时，总投资被阻断为不可评估，没有按 0 计算。
- 基线总投资 `48,250,000`，建安占比 `82.90%`。
- 土建综合单价变更后：土建工程费 `35,000,000`、建安工程费 `43,000,000`、基本预备费 `2,400,000`、总投资 `51,400,000`、建安占比 `83.66%`。
- 传播证明路径：14；受影响 Chunk：4。
- 无绑定精确数字被质量闸门阻断。
- DOCX 37,784 字节、PDF 28,495 字节，均由真实导出接口生成并保存 SHA-256。

## 导出版式验证

DOCX 的默认样式、段落样式和运行级字体均显式声明便携中文字体。服务器 PDF 可正常生成。Codex 随附的隔离渲染器没有 CJK 字体，不能作为中文显示的最终裁判；使用本机已安装的 LibreOffice 真实打开并渲染 DOCX 后，中文标题、正文和数值均正常，未出现方框字。该环境差异已保留为已知限制，而不是通过只检查“文件存在”掩盖。

## 安全与一致性

- 插件未获得数据库密码，不直连 PostgreSQL、MinIO、OpenSearch、Qdrant 或 FalkorDB。
- 日志、文档和测试产物未记录 API Key、访问 Token 或数据库密码。
- ImpactPreview 保存基线版本和校验值；基础事实变化后旧预览拒绝应用。
- 传播使用对象 ID、版本、逻辑指标键和 ComputationRun 依赖，不使用字符串全局替换。
- 局部应用不会重新生成整篇文章；未选内容保持原文并标记 stale。

## 未重复宣称的范围

此前正式妙笔链路已经真实生成完整多章节报告并通过 Chunk/引用绑定回归；本轮新增双向 Diff 后，最新浏览器重测选择了只读项目上下文和单章工具回归，没有再次通过浏览器生成整篇长报告。服务器自动化对完整文稿版本、绑定、传播和导出做了真实 API 验收。该差异在浏览器报告和已知限制中继续明确记录。
