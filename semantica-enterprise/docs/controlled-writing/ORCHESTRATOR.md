# 后端编排器

编排器执行确定性状态机：验证项目和输入 → 构建章节工作包 → 运行必要公式 → 调用 Semantica → 调用 Agent → 校验结构化返回 → 运行章节闸门 → 建立 ChunkBinding → 形成不可变版本。

章节工作包只包含本章目标、必要当前 Fact、计算结果、推演结果、Evidence、脱敏 Skeleton、缺失项和风格约束。事实过多时分页读取，不把全图塞入模型上下文。

部分章节生成只按本次 section plan 检查；完整定稿再检查全目录、跨章数字、术语、编号、附件和所有 stale/阻断项。Agent 不能跳过闸门或直接提交版本。
