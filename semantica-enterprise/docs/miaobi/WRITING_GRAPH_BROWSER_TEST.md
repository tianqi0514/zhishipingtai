# 写作图谱与妙笔浏览器测试

测试环境：`http://10.5.113.232:9002/miaobi/`。

真实浏览器完成：登录；选择“测试地区地震应急处置方案写作验收项目”；打开 V12；恢复 Plate/Yjs 文稿；验证 400 人和 100 人；验证 5 行 × 6 列任务表；打开指标修改弹窗；取消后正文不变化；检查 1280×720 与 1440×900 无整体横向溢出。

首次自动回归准确发现协同服务 `8092` 仅绑定服务器回环地址，浏览器出现 4 条 WebSocket 连接错误。测试环境改为显式内网监听并重建协同容器后复测：页面显示“协同已同步”和“2 个数据通道在线”，Console 0 error，非预期 4xx/5xx 为 0。

导出页外另行逐页复核 Word/PDF：任务表保持原生单元格，旧值 320/180 不再出现，当前值 400/100 在正文、计算结果与表格中一致。

