# 业务旅程浏览器测试

测试日期：2026-09-04

地址：`http://localhost:8080/`

视口：1280×720、1440×900

## 实际点击结果

| 场景 | 结果 |
| --- | --- |
| 新建临时知识空间 | 成功；自动设为当前空间并跳转文档列表 |
| 新空间空状态 | 成功；上传文档和选择数据源均为可点击入口 |
| 打开上传弹窗 | 成功；显示支持格式和仅检索/仅图谱/同时加工三种方式 |
| 取消上传 | 成功；弹窗关闭且不提示“请选择文件” |
| 浏览器选择文件并上传 | 成功；选择“仅检索”上传真实 `m4-test.txt`，解析 1 秒，知识加工约 69 秒并达到 100% |
| 切回验收空间 | 成功；各空间级页面立即联动 |
| 治理概览 | 成功；展示真实文档、加工方式、实体、事实、发布版本和下一步 |
| 内容治理 | 成功；标题、待处理内容和后续动作可用 |
| 白底 3D 图谱 | 成功；16 个节点、5 条边，适配和旋转按钮真实生效 |
| 规则推演 | 成功；业务页签和标题显示正确 |
| 发布记录 | 成功；当前图谱/索引版本与历史发布真实显示 |
| 浏览器后退/前进 | 成功；恢复页面和知识空间 |
| 页面刷新 | 成功；恢复合法登录、页面和知识空间 |
| 20 次连续导航 | 成功；无重复请求和页面串写 |
| 响应式与溢出 | 成功；两种视口均无非预期横向滚动 |
| Console | 0 个页面错误，0 个未处理 Promise |

临时浏览器验收空间已通过 API 清理；浏览器上传的 `m4-test.txt` 在任务达到 100% 后也已删除。原有客户数据、演示空间和 Docker Volume 未删除。

## 真实后台补充验证

在“知识加工方式验收空间”上传三份真实文件并轮询任务：

| 加工方式 | 耗时 | 图谱语义模型请求 | 结果 |
| --- | ---: | ---: | --- |
| 仅检索 | 41.66 秒 | 0 | 全文/向量可召回，图谱不产生投影 |
| 仅图谱 | 60.45 秒 | 1 | 图谱可查询，不进入检索索引 |
| 检索 + 图谱 | 67.00 秒 | 1 | 两类投影均可用 |

测试还发现一次云端模型超时后任务被错误标记为成功。Worker 已修正为“失败 + 可重试”，保留错误码和成功阶段结果；重新运行后上述三种模式全部通过。
